model User {
  id         String   @id @default(uuid())
  name       String
  email      String   @unique
  password   String
  type       String   @default("USER")
  created_at DateTime @default(now())
}